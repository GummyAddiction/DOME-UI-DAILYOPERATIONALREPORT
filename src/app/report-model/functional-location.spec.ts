import { FunctionalLocation } from './functional-location';

describe('FunctionalLocation', () => {
  it('should create an instance', () => {
    expect(new FunctionalLocation()).toBeTruthy();
  });
});
